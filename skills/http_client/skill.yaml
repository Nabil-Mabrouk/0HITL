name: http_client
description: Client HTTP structure pour APIs, metadonnees de reponse et telechargements dans le workspace.
version: 1.0.0
author: 0-HITL-Core
required_permissions:
  - network
  - filesystem
docker_image: python:3.12-slim
